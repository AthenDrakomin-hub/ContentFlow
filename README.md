# ContentFlow 跨境智汇

ContentFlow 是一个综合性的跨境内容发布仪表板，具有任务跟踪、AI 辅助内容生成和多平台调度功能。

## 🚀 部署到 Vercel (详细指南)

本项目已配置为标准的 Vite React 应用，非常适合部署到 Vercel。

### 1. 导入项目
1. 将代码推送到 GitHub、GitLab 或 Bitbucket。
2. 在 Vercel 仪表板中点击 "Add New..." -> "Project"。
3. 选择您的 `contentflow-app` 仓库并点击 "Import"。

### 2. 构建配置 (Build Settings)
Vercel 通常会自动检测 Vite 框架，默认配置如下（无需修改）：
*   **Framework Preset**: `Vite`
*   **Root Directory**: `./`
*   **Build Command**: `npm run build`
*   **Output Directory**: `dist`

### 3. 环境变量配置 (关键步骤！)
为了让 AI 接口正常工作，您**必须**在 Vercel 的 "Environment Variables" 部分添加以下变量：

| Key | Value | 说明 |
| :--- | :--- | :--- |
| `VITE_API_KEY` | `您的_Google_Gemini_API_Key` | **必须以 VITE_ 开头**，否则前端无法读取 |

> **提示**: 如果您在代码中使用的是 `process.env.API_KEY`，我们在 `vite.config.js` 中已经做了映射，但在 Vercel 后台配置时，变量名建议直接使用 `VITE_API_KEY` 以确保最佳兼容性。

## 🔑 接口问题排查 (Interface Issues)

如果部署后 AI 功能无法使用（例如点击优化按钮无反应或报错），请按以下步骤排查：

### 1. 检查 API Key 是否生效
*   在浏览器中按 `F12` 打开开发者工具。
*   切换到 **Network (网络)** 标签页。
*   尝试点击 "AI 优化" 按钮。
*   查看发出的请求（通常是 `generateContent` 相关）。
*   **400/401/403 错误**: 说明 API Key 无效、缺失或没有额度。请检查 Vercel 环境变量是否拼写正确（`VITE_API_KEY`）。
*   **500 错误**:Google 服务端问题，稍后重试。

### 2. 环境变量前缀问题
**这是最常见的问题**。Vite 构建的前端应用只能读取以 `VITE_` 开头的环境变量。
*   ❌ 错误配置: Key = `API_KEY`
*   ✅ 正确配置: Key = `VITE_API_KEY`

### 3. 浏览器控制台日志
查看 **Console (控制台)** 标签页。如果看到 `Uncaught Error: 未找到 API Key`，说明代码没有读取到变量。请重新部署项目（修改环境变量后需要 Redeploy 才能生效）。

## 🛠 本地开发

```bash
# 安装依赖
npm install

# 创建 .env.local 文件并添加 API Key
echo "VITE_API_KEY=your_key_here" > .env.local

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```